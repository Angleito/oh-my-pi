{{#when kind "==" "merge-failed"}}Merge failed: {{message}}{{#if captureError}}; patch capture failed: {{captureError}}{{/if}}.{{#if rescueBranch}} The agent's commits are preserved on branch {{rescueBranch}} — merge or cherry-pick it manually.{{/if}}{{#if retainedDir}} Isolation workspace retained at {{retainedDir}} — recover the changes from it; `omp worktree clear` reclaims it once this session has exited.{{/if}}{{/when}}
{{#when kind "==" "patch-capture-failed"}}Patch capture failed: {{message}}. Isolation workspace retained at {{retainedDir}} — recover the changes from it; `omp worktree clear` reclaims it once this session has exited.{{/when}}
{{#when kind "==" "nested-capture-failed"}}Nested patch capture failed: {{message}}. Isolation workspace retained at {{retainedDir}} — recover the changes from it; `omp worktree clear` reclaims it once this session has exited.{{/when}}
